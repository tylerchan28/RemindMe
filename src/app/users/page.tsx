export default function Users() {
    return (
      <main>
          <div>Users</div>
      </main>
    );
  }
  
  // because this is page.tsx under the users folder
  // it creates a route under http://url.com/users